const Form = () => {
    return (
        <form>
            <input placeholder="Inserte la tarea a realizar." />
            <button>Agregar Tarea</button>
        </form>
    )
};

export default Form;